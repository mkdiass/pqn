"use client";
import Link from "next/link";
import { ArrowRight, Eye, EyeOff, LockKeyhole, ShieldCheck } from "lucide-react";
import { useState } from "react";
import type { FormEvent } from "react";
import { useRouter } from "next/navigation";
export default function LoginPage(){const router=useRouter();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[showPassword,setShowPassword]=useState(false);const[pending,setPending]=useState(false);const[error,setError]=useState("");async function handleSubmit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");if(!email||!password){setError("Preencha seu e-mail e senha para continuar.");return}setPending(true);try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await r.json();if(!r.ok){setError(data.message??"Não foi possível entrar.");setPending(false);return}router.push("/cliente/dashboard");router.refresh()}catch{setError("Não foi possível conectar ao serviço de autenticação.");setPending(false)}}return <main className="pp-auth-page"><section className="pp-auth-art"><div><span className="pp-eyebrow"><i/> CENTRAL DO CLIENTE</span><h1>Sua conexão.<br/><span>Seu controle.</span></h1><p>Consulte seu plano, acompanhe sua conexão, veja faturas e fale com a Parque Net em um só lugar.</p><div className="pp-proof"><div><ShieldCheck size={19}/><small>SESSÃO PROTEGIDA</small></div><div><LockKeyhole size={19}/><small>ACESSO SEGURO</small></div></div></div><div className="pp-auth-orb"/></section><section className="pp-auth-card"><div className="pp-login-box"><span className="pp-eyebrow"><i/> PARQUE NET</span><h2>Bem-vindo de volta.</h2><p>Acesse sua Central do Cliente para continuar.</p><form onSubmit={handleSubmit}><div className="pp-field"><label htmlFor="email">E-mail</label><input id="email" type="email" autoComplete="email" placeholder="voce@email.com" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="pp-field"><label htmlFor="password">Senha</label><div style={{position:"relative"}}><input id="password" type={showPassword?"text":"password"} autoComplete="current-password" placeholder="Sua senha" value={password} onChange={e=>setPassword(e.target.value)} style={{width:"100%",paddingRight:50}}/><button type="button" aria-label="Mostrar ou ocultar senha" onClick={()=>setShowPassword(v=>!v)} style={{position:"absolute",right:12,top:13,border:0,background:"transparent",color:"#64748b",cursor:"pointer"}}>{showPassword?<EyeOff size={19}/>:<Eye size={19}/>}</button></div></div>{error&&<div className="pp-auth-demo" role="alert">{error}</div>}<button className="pp-login-submit" type="submit" disabled={pending}>{pending?"Autenticando...":<>Entrar <ArrowRight size={16}/></>}</button></form><div className="pp-form-links"><Link href="/">Voltar ao site</Link><a href="#recuperar" onClick={e=>{e.preventDefault();alert("A recuperação de senha será conectada ao sistema de clientes na próxima etapa.")}}>Esqueci minha senha</a></div><div className="pp-auth-demo"><strong>Acesso demonstrativo</strong><br/>cliente@parquenet.com.br<br/>Senha: ParqueNet@2026</div></div></section></main>}
